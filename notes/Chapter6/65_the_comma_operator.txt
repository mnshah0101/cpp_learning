6.5 The Comma Operator

The comma operator allows you to evlauation multiple expressions where a single expression is allowe,
It returns the result of the right operands

The comma has the lowest precedence of all the operators even lower than assignment:

z = (a,b) //first get the result of b, then assign that value to a variable

z = a, b //evaluates x= a, then b is evaled and discarded

Avoid using the comma expcept inside for loops

Comma can be a seperator 
